using UnityEngine;
using System.Collections;

public class AttachWeaponScript : MonoBehaviour {

	enum WeaponTypes { None, LieGenerator, DrinkSpot, ShoppingCenter };

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
